% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adept_find_walking.R
\name{adept_find_walking}
\alias{adept_find_walking}
\title{Finds walking and calculate steps from raw acceleration data.}
\usage{
adept_find_walking(data, sample_rate, template = NULL, ..., verbose = TRUE)
}
\arguments{
\item{data}{A \code{data.frame} with a column for time in \code{POSIXct} (usually
\code{HEADER_TIMESTAMP}), and \code{X}, \code{Y}, \code{Z}}

\item{sample_rate}{sampling frequency (in Hz) \strong{for the data}.}

\item{...}{Additional arguments to pass to \code{\link[adept:segmentWalking]{adept::segmentWalking()}}}

\item{verbose}{print diagnostic messages}
}
\value{
A vector of number of steps per second
}
\description{
Method finds periods of repetitive and continuous oscillations with
predominant frequency occurring within know step frequency range.
Frequency components are extracted with Continuous Wavelet Transform.
}
\examples{
csv_file = system.file("test_data_bout.csv", package = "walking")
if (requireNamespace("readr", quietly = TRUE) && have_forest()) {
  x = readr::read_csv(csv_file)
  colnames(x)[colnames(x) == "UTC time"] = "time"
  res = adept_find_walking(data = x, sample_rate = 15L)
}
# Running with the matrix
}
