% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/resample_accel_data.R
\name{resample_accel_data}
\alias{resample_accel_data}
\alias{resample_accel_data_to_time}
\title{Resample 3-axial input signal to a specific sampling rate}
\usage{
resample_accel_data(data, sample_rate, ...)

resample_accel_data_to_time(data, times, ...)
}
\arguments{
\item{data}{A \code{data.frame} with a column for time in \code{POSIXct} (usually
\code{HEADER_TIME_STAMP}), and \code{X}, \code{Y}, \code{Z}}

\item{sample_rate}{sampling frequency, coercible to an integer.
This is the sampling rate you're sampling the data \emph{into}.}

\item{...}{additional arguments to pass to \code{\link[stats:approxfun]{stats::approx()}}}

\item{times}{a vector of \code{POSIXct} date/time values to interpolate
the data to}
}
\value{
A \code{data.frame}/\code{tibble} of \code{HEADER_TIME_STAMP} and \code{X}, \code{Y}, \code{Z}.
}
\description{
Resample 3-axial input signal to a specific sampling rate
}
\examples{
options(digits.secs = 3)
csv_file = system.file("test_data_bout.csv", package = "walking")
if (requireNamespace("readr", quietly = TRUE)) {
  x = readr::read_csv(csv_file)
  colnames(x)[colnames(x) == "UTC time"] = "time"

  res = resample_accel_data(data = x, sample_rate = 80)
  res = resample_accel_data(data = x, sample_rate = 100)
  res = resample_accel_data(data = x, sample_rate = 1)
}
}
